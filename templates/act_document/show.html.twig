{% extends 'base.html.twig' %}

{% block title %}ActDocument{% endblock %}

{% block body %}
    <h1>ActDocument</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ act_document.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ act_document.name }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ act_document.type }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ act_document.comment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('act_document_index') }}">back to list</a>

    <a href="{{ path('act_document_edit', {'id': act_document.id}) }}">edit</a>

    {{ include('act_document/_delete_form.html.twig') }}
{% endblock %}