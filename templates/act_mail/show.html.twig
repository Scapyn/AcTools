{% extends 'base.html.twig' %}

{% block title %}ActMail{% endblock %}

{% block body %}
    <h1>ActMail</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ act_mail.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ act_mail.description }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ act_mail.date ? act_mail.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ act_mail.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('act_mail_index') }}">back to list</a>

    <a href="{{ path('act_mail_edit', {'id': act_mail.id}) }}">edit</a>

    {{ include('act_mail/_delete_form.html.twig') }}
{% endblock %}