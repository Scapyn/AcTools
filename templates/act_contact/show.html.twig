{% extends 'base.html.twig' %}

{% block title %}ActContact{% endblock %}

{% block body %}
    <h1>ActContact</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ act_contact.id }}</td>
            </tr>
            <tr>
                <th>Contact</th>
                <td>{{ act_contact.contact }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ act_contact.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('act_contact_index') }}">back to list</a>

    <a href="{{ path('act_contact_edit', {'id': act_contact.id}) }}">edit</a>

    {{ include('act_contact/_delete_form.html.twig') }}
{% endblock %}