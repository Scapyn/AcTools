<form method="post" action="{{ path('act_event_delete', {'id': act_event.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ act_event.id) }}">
    <button class="btn">Delete</button>
</form>