{% extends 'base.html.twig' %}

{% block title %}Edit ActEvent{% endblock %}

{% block body %}
    <h1>Edit ActEvent</h1>

    {{ include('act_event/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('act_event_index') }}">back to list</a>

    {{ include('act_event/_delete_form.html.twig') }}
{% endblock %}