{% extends 'base.html.twig' %}

{% block title %}ActEvent{% endblock %}

{% block body %}
    <h1>ActEvent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ act_event.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ act_event.name }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ act_event.date ? act_event.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ act_event.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('act_event_index') }}">back to list</a>

    <a href="{{ path('act_event_edit', {'id': act_event.id}) }}">edit</a>

    {{ include('act_event/_delete_form.html.twig') }}
{% endblock %}